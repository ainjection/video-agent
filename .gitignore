node_modules
dist
.DS_Store
.env
.env.local

# Rendered outputs
out
remotion-bundle
.remotion

# User-uploaded assets (potentially sensitive / large)
dashboard/data/audio
dashboard/data/images
public/audio-agent
public/images

# Large generated caches
dashboard/data/thumbnails

# Per-user runtime data (contain absolute paths, not useful in a clone)
dashboard/data/render-history.json
dashboard/data/props/
dashboard/data/timeline-projects.json

# Editor / OS
.vscode
*.log
